@using Domain.App_GlobalResources;
@{
    ViewBag.Title = @CommonResources.lblConfirmation;
}
<h2>@CommonResources.ConfirmationEmailSent</h2>

<div class="row-fluid">
    <div style="text-align:center">
        <h3>
            <span class="text-info">
                @Html.Raw(ViewBag.ConfirmationMessage)
            </span>
            <br/><br/>
            <span class="text-info">
                @Html.Raw(ViewBag.ConfirmationMessage2)
            </span>

        </h3>
    </div>
</div>
